name: aemo_energy
version: "1.0.0"
config-version: 2

profile: aemo_energy

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  aemo_energy:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
